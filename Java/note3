类的继承

计算机中经常使用类的继承关系来表示对象之间的分类关系。

super 父类，调用父类构造方法，super放在第一行

this引用，实实在在存在，可以作为函数参数，作为返回值，但super只是关键字，不能作为参数和返回值。

子类对象可以赋值给父类引用变量，这是多态，实际执行调用子类实现，这是动态绑定。

继承和多态的基本概念是比较简单的，子类继承父类，自动拥有父类的属性和行为，并可拓展属性和行为，同时，重写父类的方法以修改行为

继承细节
子类通过super调用父亲的构造方法，若没有通过super调用，则自动调用父类的默认构造方法。

重名与静态绑定
子类可以重写父类非private的方法，当调用，会动态绑定，执行子类的方法。实例变量，静态方法和静态变量可以重名吗？
可以重名，重名后有两个变量，private变量和方法只能在类中访问。

静态变量和静态方法一般通过类名直接访问，但也可以通过类的对象访问，程序输出为：
static_base
base
base static: static_base
child_base
child
child static:child_base


